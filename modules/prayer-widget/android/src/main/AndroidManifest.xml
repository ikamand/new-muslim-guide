<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!--
    The three home-screen widgets (docs/widgets.md) and the receiver that
    redraws them. Each widget may also be placed on the lock screen on
    Android 16 and later (widgetCategory keyguard). Redraws are exact alarms,
    which app.json already declares for the adhan.
  -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <receiver
      android:name=".NichePrayerWidget"
      android:exported="true"
      android:label="@string/prayer_widget_name_niche">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/prayer_widget_niche_info" />
    </receiver>

    <receiver
      android:name=".RowPrayerWidget"
      android:exported="true"
      android:label="@string/prayer_widget_name_row">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/prayer_widget_row_info" />
    </receiver>

    <receiver
      android:name=".QuietPrayerWidget"
      android:exported="true"
      android:label="@string/prayer_widget_name_quiet">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/prayer_widget_quiet_info" />
    </receiver>

    <receiver
      android:name=".PrayerWidgetRefresh"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        <action android:name="android.intent.action.TIME_SET" />
        <action android:name="android.intent.action.TIMEZONE_CHANGED" />
        <action android:name="android.intent.action.LOCALE_CHANGED" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
