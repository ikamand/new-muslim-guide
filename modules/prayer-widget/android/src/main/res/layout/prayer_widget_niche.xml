<?xml version="1.0" encoding="utf-8"?>
<!--
  The niche (docs/widgets.md). The arch and its marks are a bitmap drawn from
  the schedule; the words are the card's, set at every redraw. Only classes
  RemoteViews allows: FrameLayout, LinearLayout, ImageView, TextView.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@android:id/background"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/prayer_widget_ground"
  android:clipToOutline="true">

  <ImageView
    android:id="@+id/prayer_widget_arch"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:importantForAccessibility="no"
    android:scaleType="fitCenter" />

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingStart="22dp"
    android:paddingEnd="22dp">

    <FrameLayout
      android:layout_width="match_parent"
      android:layout_height="0dp"
      android:layout_weight="40" />

    <TextView
      android:id="@+id/prayer_widget_caption"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:letterSpacing="0.12"
      android:maxLines="1"
      android:textAllCaps="true"
      android:textColor="@color/prayer_widget_text_secondary"
      android:textSize="9sp" />

    <TextView
      android:id="@+id/prayer_widget_name"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:fontFamily="@font/literata_semibold"
      android:maxLines="1"
      android:text="@string/prayer_widget_name_niche"
      android:textColor="@color/prayer_widget_text"
      android:textSize="20sp" />

    <TextView
      android:id="@+id/prayer_widget_time"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:fontFeatureSettings="tnum"
      android:maxLines="1"
      android:textColor="@color/prayer_widget_gold"
      android:textSize="12sp"
      android:textStyle="bold" />

    <TextView
      android:id="@+id/prayer_widget_message"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:gravity="center"
      android:maxLines="3"
      android:textColor="@color/prayer_widget_text_secondary"
      android:textSize="11sp"
      android:visibility="gone" />

    <FrameLayout
      android:layout_width="match_parent"
      android:layout_height="0dp"
      android:layout_weight="24" />
  </LinearLayout>
</FrameLayout>
