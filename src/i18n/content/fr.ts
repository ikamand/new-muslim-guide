import type { ContentDict } from '../locales';

/**
 * French translations of the app's religious content.
 *
 * ⚠️ EMPTY ON PURPOSE. Nothing here may be machine-translated or written from
 * memory — these are translations of Qur'an, of dhikr said in prayer, and of
 * instructions on how to worship. Getting one wrong teaches someone to pray
 * incorrectly in a language neither of us reads back.
 *
 * Fill it from a qualified French translator working off
 * `docs/i18n-manifest.csv`, which lists every key with its English text.
 * Anything absent falls back to English, so partial delivery is safe to ship.
 */
export const FR: ContentDict = {};
